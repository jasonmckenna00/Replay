import {connect} from 'react-redux'
import VideoShow from './video_show'


const msp = state => ({

})

const mdp = dispatch => ({

})

const VideoShowContainer = connect(null,null)(VideoShow)

export default VideoShowContainer