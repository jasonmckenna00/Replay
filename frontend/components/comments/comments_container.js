import {connect} from 'react-redux'
import Comments from './comments'
const msp = state =>{
    return{

    }
}

const mdp = dispatch => {

    return{

    }
}

export default connect(msp,mdp)(Comments)